class Taxi {
    constructor(id, text, distance, distance_desc, duration){
            this.id = id;
            this.text = text;
            this.distance = distance; 
            this.distance_desc = distance_desc;
            this.duration = duration;
        }
    }

export default Taxi;