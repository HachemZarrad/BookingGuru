class Flight {
    constructor(id, geoId, destination, image, latitude, longitude, name){
            this.id = id;
            this.geoId = geoId;
            this.destination = destination;
            this.image = image;
            this.latitude = latitude;
            this.longitude = longitude;
            this.name = name; 
        }
    }

export default Flight;