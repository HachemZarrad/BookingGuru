class User {
    constructor(id, firstname, lastname, admin, username){
            this.id = id;
            this.firstname = firstname;
            this.lastname = lastname;
            this.admin = admin;
            this.username = username; 
        }
    }


export default User;